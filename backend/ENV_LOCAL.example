# Backend - Local/Offline (SQLite)
# Configuración para modo OFFLINE
# Copia este archivo como .env.local y ajusta los valores

# Base de Datos SQLite (Modo Offline)
DATABASE_URL_SQLITE="file:./prisma/local.db"

# Modo de Base de Datos
DB_MODE=offline

# Puerto del Servidor
PORT=8089

# Host (0.0.0.0 permite acceso desde la red local)
HOST=0.0.0.0

# CORS - Orígenes permitidos (separados por comas)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,file://

# JWT Secret (mismo que en desarrollo para consistencia)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui_cambiar_en_produccion

# Entorno
NODE_ENV=development

